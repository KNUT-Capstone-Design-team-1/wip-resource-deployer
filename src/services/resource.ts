/**
 * 리소스 파일을 읽는다
 */
async function readResourceFiles() {}

/**
 * 리소스 파일을 JSON 데이터로 변환한다
 */
async function convertResourceFileToJSON() {}

/**
 * 리소스 데이터를 리소스 레지스트리에 업로드 한다
 */
async function handleUploadResources() {}

/**
 * 리소스 파일을 읽어 데이터베이스에 저장 후 데이터베이스 파일을 리소스 레지스트리에 업로드 한다
 */
export async function uploadResources() {}
